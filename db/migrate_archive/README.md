# Archived Migrations

This directory stores the pre-squash migration history.

- Active schema source of truth: `db/structure.sql`
- Active migration directory: `db/migrate/`
- Bootstrap command for new/empty databases: `bin/rails db:prepare`

These archived files are kept for reference only and are not executed by Rails.
