<% content_for :title, "Admin · Edit Newsletter Campaign" %>

<div class="w-full">
  <div class="flex items-center justify-between mb-4">
    <h1 class="text-2xl font-bold">Edit Newsletter Campaign</h1>
    <%= link_to "Back to Campaign", admin_newsletter_campaign_path(@campaign), class: "rounded bg-gray-600 px-3 py-2 text-white hover:bg-gray-700" %>
  </div>

  <%= render "form" %>
</div>
