<% content_for :title, "Admin · #{@reference.name}" %>

<div class="w-full max-w-4xl">
  <div class="mb-4 flex flex-wrap items-center justify-between gap-2">
    <h1 class="text-2xl font-bold"><%= @reference.name %></h1>
    <div class="flex gap-2">
      <%= link_to "Back to References", admin_references_path, class: "rounded border border-gray-300 px-3 py-2 hover:bg-gray-50" %>
      <%= link_to "Edit", edit_admin_reference_path(@reference), class: "rounded bg-blue-600 px-3 py-2 text-white hover:bg-blue-700" %>
      <%= button_to "Delete", admin_reference_path(@reference), method: :delete, class: "rounded bg-red-600 px-3 py-2 text-white hover:bg-red-700", data: { turbo_confirm: "Delete this reference?" } %>
    </div>
  </div>

  <div class="rounded border bg-white p-4">
    <p class="text-sm text-gray-600">
      <strong>Author:</strong> <%= @reference.author.presence || "Unknown" %>
    </p>
    <p class="mt-1 text-sm text-gray-600">
      <strong>Publish Year:</strong> <%= @reference.publish_date&.year || "Not set" %>
    </p>
    <div class="mt-4 whitespace-pre-wrap text-gray-800">
      <%= @reference.description.presence || "No description provided." %>
    </div>
  </div>
</div>
